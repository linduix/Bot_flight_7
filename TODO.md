# TODO

- **Network inputs**: missile-style guidance features — swap `delta_dist`, `vel`, `old_vel` for `dir` (unit vector to target), `los_rate` (line-of-sight rotation rate), `tti` (time-to-intercept), `delta_tti`, and `integrated_dir`. Adjust input count and downstream layer sizes accordingly.
- **Scale-invariant inputs**: make all network inputs scale-invariant so the controller generalizes across spawn distances and chain lengths without retraining.
- **Adaptive homing reward (research)**: investigate a two-phase hybrid reward — violent predator dive from long range, smooth transition to a glued shadow at touch — merged by a TTI-driven sigmoid blend (no hard distance cliffs). Predator phase rewards aggressive closing along LOS; shadow phase rewards par + perp velocity matching against the target. Note: LOS rate, TTI etc. are network *inputs* (see above), not reward terms — the reward stays defined over velocity errors. Reference: Proportional Navigation with Adaptive Terminal Guidance literature.
  - **Idea reminder — experimentally proven reward design**: In modern drone control literature, this is solved by splitting the continuous step reward into three core pillars: Trajectory Guidance (ZEM), Velocity Alignment (ZEV), and an Exponential Proximity Bonus.

- **Robustness via domain randomization (food for thought)**: agents overfit to specific paths; to survive garbage/antagonistic targets, generate procedural chaos every eval epoch:
  - *Step-function teleportation*: every few dozen ticks, snap the target to a random position — forces instant recovery from huge error spikes without destabilizing.
  - *Frequency/jerk injection*: target oscillates via high-frequency sine / high-acceleration noise — simulates a target actively trying to break the control loop.
  - *Physics overmatch*: make the target faster/more agile than the drone in ~20% of episodes — forces a "best-effort / optimal corner-cutting" strategy instead of giving up when it physically can't keep up.
- **Reality-bounded ideal velocity (food for thought)**: instead of scoring against the raw `ideal_vel` (which can demand an unreachable instantaneous velocity), integrate along that vector using the drone's max achievable acceleration — i.e. cap the ideal to `v_prev + clamp(ideal_vel - v_prev, max_a*dt)`. Makes the setpoint physically reachable each tick so max-effort = max score, and pairs naturally with physics-overmatch randomization (rewards best effort when the target can't be matched). Have `prev_vel` tracked already.
- **Research: SRR training** — look into SRR training and whether it applies here.
- **Research: inverse dynamics solver for ideal actions** — given a desired (reality-bounded/lead) ideal acceleration or velocity, solve the inverse dynamics for the thruster/gimbal commands that would produce it (thrust magnitudes + gimbal angles, accounting for underactuation + attitude). Use as a reference/teacher signal for reward shaping or warm-starting, not as the deployed controller.
- **CMA-ES condition-number tracking (stagnation indicator)**: monitor the condition number of the CMA covariance (ratio of largest/smallest eigenvalue, or axis ratio) per arm. A blowing-up condition number signals the search has collapsed to a degenerate ridge / lost exploration in most dims -> use as a stagnation/restart trigger alongside should_stop and the stale-seed check.
- **Datalogging + graphs**: log and plot (a) fitness vs wall-clock time and (b) inter-generation variance vs time, to make "is it actually improving?" readable instead of eyeballing gen prints. Persist the series so runs can be compared across configs.
- **Lead / non-tail-chasing ideal velocity (food for thought)**: current `ideal_vel = v_target + safe_v_term*los_u` aims at the target's CURRENT position -> pursuit-curve tail chase against a moving target. Make it an intercept: aim the approach budget at the target's FUTURE position. Use the **actual** future chain position (privileged-information reward shaping — the reward may use ground-truth `tick_pos` the network can't see; only network *inputs* must stay estimate-based and future-blind). Sketch: `t_go = dist/safe_v`, `lead_ticks = clip(ticks + t_go/dt, 0, n_ticks-1)`, `lead_pos = tick_pos[arangeS, lead_ticks]`, point `los_lead` at `lead_pos`, and match the target's future velocity there. Reward-only (no retrain-for-arch). Caveats: never leak future into inputs; `t_go` via dist/safe_v is approximate; pre-touch the chain is static so lead==current automatically.
